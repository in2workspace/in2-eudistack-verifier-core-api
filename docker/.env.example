# Verifier Core API — Environment Variables
# Copy to .env and fill in values for production deployment.
# All values below are optional — the app starts with dev defaults if omitted.

# Backend URL (public-facing URL of this service)
# VERIFIER_BACKEND_URL=https://verifier.example.com

# Cryptographic identity (leave empty to auto-generate ephemeral P-256 key)
# VERIFIER_BACKEND_IDENTITY_PRIVATEKEY=0x...
# VERIFIER_BACKEND_IDENTITY_DIDKEY=did:key:z...

# Trust Framework: Trusted Issuers (leave empty to use local YAML)
# VERIFIER_BACKEND_TRUSTFRAMEWORKS_0_TRUSTEDISSUERSLISTURL=https://api-pilot.ebsi.eu/trusted-issuers-registry/v4/issuers/

# Trust Framework: Client Registry (leave empty to use local YAML)
# VERIFIER_BACKEND_TRUSTFRAMEWORKS_0_TRUSTEDSERVICESLISTURL=https://raw.githubusercontent.com/.../trusted_services_list.yaml

# Frontend URLs
# VERIFIER_FRONTEND_URLS_ONBOARDING=https://portal.example.com/onboarding
# VERIFIER_FRONTEND_URLS_SUPPORT=https://portal.example.com/support
# VERIFIER_FRONTEND_URLS_WALLET=https://wallet.example.com
